export interface ITransactions{
    id: number
    amount: number
    currency: string
    userId: number
    type: string
    connectedCard: string
    connectedUser: string
    createdAt: string
    updatedAt: string
}